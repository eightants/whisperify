{
  "version": 2, 
  "env": {
    "CLIENT_ID": "@client-id", 
    "CLIENT_SECRET": "@client-secret", 
    "MONGO": "@mongo"
  },
  "build": {
    "env": {
      "CLIENT_ID": "@client-id", 
      "CLIENT_SECRET": "@client-secret",
      "MONGO": "@mongo"
    }
  }, 
  "builds": [{ "src": "server.js", "use": "@now/node-server" }], 
  "routes": [{ "src": "/(.*)", "dest": "/server.js" }]
}